# Environment variables for pipeline: build.
#
# Usage: wercker --environment envs/build.env build (from the root of the project)
#
# Environment variables need to start with either X_ or XXX_. Private
# environment variables cannot be downloaded from the API and thus have been
# left empty and are prefixed with XXX_

X_DOCKER_USERNAME='odx-pipelines/guoyong.zhang@oracle.com'
XXX_DOCKER_PASSWORD=''
